name: churn-mlflow-project

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path:
        type: path
        default: MLProject/churn-bigml-80_preprocessing/churn_processed.csv
    command: >
      python modelling.py --data_path {data_path}